/*
have a := 10;

if (a < 1) {
	CSPrint(a is less than 1)
} else {
	CSPrint(a is greater than 5)
} */

func main (a) {
	have a := Random(1, 10);

	return a
};

have result := main(10);

CSPrint(result)
